# ==============================================================================
# .pre-commit-config.yaml – Stemma
#
# Setup (once per clone): pre-commit install
# Run everything: pre-commit run --all-files
# Update hooks: pre-commit autoupdate
# ==============================================================================

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      # Cosmetic rewrites follow release code/config, not research or vendors.
      - id: trailing-whitespace  # Remove trailing whitespace
        args: [--markdown-linebreak-ext=md]
        files: '^([^/]+|(src|tests|data|reproduce|\.github)/.*)$'
        exclude: ^PROJECT\.md$
      - id: end-of-file-fixer  # Ensure files end with a newline
        files: '^([^/]+|(src|tests|data|reproduce|\.github)/.*)$'
        exclude: ^PROJECT\.md$
      - id: check-merge-conflict  # Catch unresolved merge conflict markers
      - id: check-added-large-files  # Block large files from being committed
        args: [--maxkb=5000]
      - id: detect-private-key  # Basic protection against committing private keys
      - id: check-toml  # Validate TOML syntax
      - id: check-yaml  # Validate YAML syntax
      # Optional checks for repositories that use these file types/platforms.
      # - id: check-json
      # - id: check-symlinks
      # - id: check-case-conflict

# Ruff – keep the revision aligned with the dev dependency.
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.16.8
    hooks:
      - id: ruff-check
        args: ["--fix"]
        files: ^(src|tests|reproduce)/
      - id: ruff-format
        files: ^(src|tests|reproduce)/

# ==============================================================================
# Optional tools – enable when the project uses them
# ==============================================================================

# Strip notebook outputs; keep publication figures in reproduce/.
#  - repo: https://github.com/kynan/nbstripout
#    rev: 0.9.1
#    hooks:
#      - id: nbstripout

# Lint shell scripts.
#  - repo: https://github.com/shellcheck-py/shellcheck-py
#    rev: v0.11.0.1
#    hooks:
#      - id: shellcheck

# Normalize pyproject.toml after settling its structure and comments.
#  - repo: https://github.com/tox-dev/pyproject-fmt
#    rev: v2.29.4
#    hooks:
#      - id: pyproject-fmt
